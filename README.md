In this assignment, I first created a Windows EC2 instance and started it. After the instance was running, I attempted to connect to it using RDP.

During the connection process, I found that I needed to download the RDP client file. After downloading and opening the file, it prompted me to enter the password. I selected the option to generate the password, uploaded the `.pem` key file, and clicked on decrypt to retrieve the password.

I copied the generated password and used it to complete the RDP connection. Once connected, the Windows virtual machine opened successfully. I then opened Command Prompt (CMD) inside the Windows VM and executed the `systeminfo` command to check the system details. I also ran the `hostname` command to verify the machine name.

I would appreciate your feedback on this assignment.
